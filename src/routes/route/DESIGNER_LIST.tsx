import { PATH } from "@/constants/path";
import DesignerListPage from "@/pages/DesignerListPage";

const DESIGNER_LIST_ROUTES = [
	{
		path: PATH.designerList,
		element: <DesignerListPage />,
	},
];

export default DESIGNER_LIST_ROUTES;
